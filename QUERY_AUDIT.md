# QUERY_AUDIT.md — аудит парсинга по 30 сложным запросам

Прогон `scripts/query_smoke.py` (30 запутанных запросов) через `POST /build-url`
с `AI_ENRICHMENT_ENABLED=false` и замоканным валидатором pik.ru (сеть не
используется, `count=42`). Сервер не поднимался — драйвер `fastapi.testclient.TestClient`.

Воспроизвести:

```bash
AI_ENRICHMENT_ENABLED=false uv run python scripts/query_smoke.py
```

**Легенда вердиктов:**

- ✅ — распознано корректно, URL валиден;
- ⚠️ — ограничение окружения/данных (ИИ выключен, нет GUID/id в справочнике,
  нет POI-категории) — **не баг кода**, поведение ожидаемое и отражено в warnings;
- 🟡 — недочёт (мелкая некорректность/шум, ссылка в целом рабочая);
- 🔴 — дефект (молча теряется или искажается критерий).

## Таблица: запрос → ожидание → факт → вердикт

| # | Запрос | Ожидание | Факт (URL / фильтры) | Вердикт |
|---|--------|----------|----------------------|---------|
| 01 | хочу двушку у метро до 15 млн с отделкой | 2к, ≤15 млн, отделка, близость к метро | `/two-room/finish?priceTo=15000000&timeOnFoot=15`; rooms=2, price_max=15млн, finish, **time_on_foot=15** | 🟡 лишний `timeOnFoot=15` — правило времени пешком «съело» число из «15 млн» |
| 02 | трёшка до 20 лямов, подешевле | 3к, ≤20 млн, сортировка по цене ↑ | `/three-room?priceTo=20000000&sortBy=price&orderBy=asc`; rooms=3+, sort=price_asc | ✅ («лямов» → млн, «подешевле» → asc) |
| 03 | однушка за 8000000 без отделки | 1к, ≤8 млн, без отделки | `/one-room/bez-otdelki`; rooms=1, finish=без отделки; **price_max отсутствует** («за 8000000» → warning) | 🔴 цена 8 000 000 ₽ потеряна |
| 04 | студия до 6.5 млн рядом с метро | студия, ≤6.5 млн | `/studio?priceTo=6500000`; rooms=студия, price_max=6.5млн | ✅ (дробная цена 6.5 разобрана) |
| 05 | нужна 3-комнатная от 60 метров, бюджет 25 млн | 3к, площадь ≥60, ≤25 млн | `/three-room?priceTo=25000000&areaFrom=60.0`; rooms=3+, area_min=60, price_max=25млн | ✅ |
| 06 | 2-х комнатную квартиру, цена от 10 до 18 млн | 2к, 10–18 млн | `/two-room?priceFrom=10000000&priceTo=18000000&floorFrom=10&floorTo=18` | 🔴 диапазон цены «10 до 18» продублирован в этаж (floorFrom=10&floorTo=18) |
| 07 | четырёхкомнатная, площадь побольше | 4к, сортировка площадь ↓ | `/three-room?sortBy=area&orderBy=desc`; rooms=3+ | 🟡 4-комнатная схлопнута в чип «3+» и путь `three-room` |
| 08 | 1-2 комнатные до 12 млн, чистовая отделка | 1 и 2к (multi), ≤12 млн, отделка | `/finish?rooms=1,2&priceTo=12000000`; rooms=[1,2], finish | ✅ (multi-комнатность → query `rooms=1,2`) |
| 09 | двушка бюджет 14 лямов, кухня от 12 метров | 2к, ≤14 млн, кухня ≥12 | `/two-room?priceTo=14000000&areaKitchenFrom=12.0` | ✅ (кухня отделена от общей площади) |
| 10 | трёшку за 22 млн, высокий этаж, с отделкой | 3к, ≤22 млн, не первый этаж, отделка | `/three-room/finish?priceTo=22000000&notFirstFloor=1` | ✅ («высокий этаж» → notFirstFloor) |
| 11 | квартира рядом с метро Тульская до 17 млн | ≤17 млн, метро Тульская | `?priceTo=17000000`; metro=[Тульская] — но нет slug/id → warning | ⚠️ справочник метро не докурен (GUID/slug) |
| 12 | двушка в районе Хамовники, подороже | 2к, район Хамовники, сортировка ↓ | `/two-room?sortBy=price&orderBy=desc`; «в районе Хамовники» → warning | ⚠️ района «Хамовники» нет в справочнике |
| 13 | однушка около метро ЦСКА, готовый дом | 1к, метро ЦСКА, ready | `/one-room/ready`; metro=[ЦСКА] без id → warning | ⚠️ справочник метро (нет id) |
| 14 | хочу трёшку в ЮЗАО до 30 млн | 3к, округ ЮЗАО, ≤30 млн | `/three-room/yuzao?priceTo=30000000`; counties=[ЮЗАО] | ✅ (округ → слаг в путь) |
| 15 | квартира у метро Аэропорт, с 5 по 20 этаж | этаж 5–20, метро Аэропорт | `?floorFrom=5&floorTo=20`; metro=[Аэропорт] — неоднозначность + нет id | ⚠️ неоднозначность «Аэропорт»/«Аэропорт Внуково» + нет id (данные) |
| 16 | двушка рядом с парком до 16 млн | 2к, ≤16 млн, POI парк | `/two-room?priceTo=16000000`; poi=park_forest → warning «ИИ выключено» | ⚠️ POI требует ИИ, здесь выключен |
| 17 | трёшка рядом со школой и детским садом | 3к, POI школа+сад | `/three-room`; poi=[school, kindergarten] → warning «ИИ выключено» | ⚠️ POI требует ИИ |
| 18 | квартира рядом с МГУ, до 25 млн | ≤25 млн, сужение ЖК к МГУ | `?priceTo=25000000`; landmark=МГУ (с координатами), но **ЖК в URL не сужены** | 🟡 ориентир распознан, но сужение `complexes` не применено к ссылке |
| 19 | однушка недалеко от парка, не первый этаж | 1к, POI парк, не первый этаж | `/one-room?notFirstFloor=1`; poi=park_forest; «недалеко от» → warning | 🟡 маркер «недалеко от» утёк в warning как «нераспознанное» |
| 20 | двушка возле метро и рядом с поликлиникой до 15 млн | 2к, ≤15 млн, близость метро+клиника | `/two-room?priceTo=15000000`; «возле метро», «рядом с поликлиникой» → warnings | ⚠️ нет POI-категории «поликлиника»; «возле метро» не распознано |
| 21 | двушка не первый и не последний этаж до 14 млн | 2к, ≤14 млн, не 1-й и не последний | `/two-room?priceTo=14000000&notFirstFloor=1&notLastFloor=1` | ✅ (двойное отрицание этажа) |
| 22 | квартира без апартаментов, только квартиры до 12 млн | только квартиры, ≤12 млн | `?priceTo=12000000&type=1`; housing_type=flats_only | ✅ |
| 23 | трёшка, только свободные, не бронь | 3к, только свободные | `/three-room?status=free`; only_available=True | ✅ |
| 24 | однушка без отделки, не последний этаж | 1к, без отделки, не последний | `/one-room/bez-otdelki?notLastFloor=1` | ✅ |
| 25 | хачу двушку у митро падешевле да 15 млн | 2к, ≤15 млн, (сорт ↓ — опечатка) | `/two-room?priceTo=15000000`; «хачу», «у митро падешевле да» → warnings | ⚠️ опечатки «митро/падешевле» не распознаны (толерантность к опечаткам ограничена); цена «да 15 млн» разобрана |
| 26 | трешка 800к первый взнос, ипотека, до 20 млн | 3к, ≤20 млн; 800к — НЕ цена квартиры | `/three-room?priceTo=20000000`; «800к первый взнос», «ипотека» → warnings | ✅ 800к корректно не принят за цену квартиры |
| 27 | нужна хата двушка, бюджет 13 лямов, с ремонтом | 2к, ≤13 млн, отделка | `/two-room/finish?priceTo=13000000` | ✅ («хата» — стоп-слово, «с ремонтом» → finish) |
| 28 | квартира с отдельным санузлом до 18 млн | ≤18 млн; «отдельный санузел» = раздельный (нет на pik.ru) | `?priceTo=18000000&optionGroups=manybathrooms`; «квартира с отдельным» → warning | 🔴 «отдельный санузлом» ложно сматчен в «Два и более санузла» (manybathrooms) |
| 29 | вторичка двушка до 15 млн у метро | 2к, ≤15 млн; вторичка не поддерживается | `/two-room?priceTo=15000000`; «вторичка» → warning «не поддерживается pik.ru» | ✅ (каталог неподдерживаемого) |
| 30 | панельный дом, трёшка, кирпичный, до 20 млн | 3к, ≤20 млн; материал не поддерживается | `/three-room?priceTo=20000000`; «панельный дом» → warning материала, «кирпичный» → generic warning | 🟡 «кирпичный» без слова «дом» не попал в каталог материалов (generic warning) |

## Найденные ошибки и недочёты (по убыванию серьёзности)

### 🔴 Дефекты

1. **Диапазон цены «10 до 18 млн» дублируется в этаж (запрос 06).**
   `«цена от 10 до 18 млн»` даёт корректно `priceFrom=10000000&priceTo=18000000`,
   но те же числа 10 и 18 повторно матчатся правилом этажа →
   `floorFrom=10&floorTo=18`. В выдаче появляется ложное ограничение по этажу
   (5–18 этаж), которого пользователь не просил. Причина: `extract_floor`
   ловит «от 10 до 18» без явного ключевого слова «этаж», хотя рядом стоит
   слово «цена». Самый вредный из найденных — молча сужает выдачу.
   *Рекомендация:* правило этажа должно требовать якорь «этаж»/«эт.» рядом с
   числом, либо не срабатывать на span, уже поглощённый ценой.

2. **«отдельный санузел» ложно сматчен в «Два и более санузла» (запрос 28).**
   Фраза «с отдельным санузлом» семантически = раздельный санузел (по каталогу
   промпта 24 такого фильтра на pik.ru нет). Но rapidfuzz по подстроке
   «санузлом»≈«санузла» выдал `optionGroups=manybathrooms` («Два и более
   санузла») — **противоположный по смыслу фильтр, добавленный молча**. Клиент
   получит ссылку на квартиры с 2+ санузлами вместо честного «не поддерживается».
   *Рекомендация:* поднять порог/строгую перепроверку для этой группы либо
   занести «раздельный/отдельный санузел» в каталог неподдерживаемого (24).

3. **Цена «за 8000000» теряется (запрос 03).**
   «однушка за 8000000 без отделки» — 8 000 000 ≥ порога 100 000, должно дать
   `price_max=8000000`, но число ушло в warning «не удалось распознать».
   Бюджет пользователя проигнорирован, ссылка без ценового потолка (шире, чем
   надо). Сравнить с запросом 06, где «до 10…18 млн» разбирается. Похоже,
   голое 7-значное число после предлога «за» не подхватывается ценовым правилом.
   *Рекомендация:* «за N» с большим голым числом (или суффиксом) должно давать
   `price_max`.

### 🟡 Недочёты

4. **Лишний `timeOnFoot=15` (запрос 01).** «у метро до 15 млн»: число «15» из
   «15 млн» повторно использовано правилом «время пешком до метро»
   (`timeOnFoot=15`). Появляется ограничение «15 мин пешком», которого нет в
   запросе. В запросе 04 («рядом с метро», без числа рядом) этого нет — то есть
   правило хватает ближайшее число независимо от единиц.

5. **4-комнатная схлопывается в «3+» / путь `three-room` (запрос 07).**
   «четырёхкомнатная» → чип `3+` и слаг `three-room`. Это осознанное правило
   «≥3 → 3+», но специфика «ровно 4 комнаты» теряется без предупреждения.

6. **Ориентир МГУ распознан, но не применён к ссылке (запрос 18).**
   `landmark_requirements` с координатами есть в criteria, однако сужение
   `complexes` по дистанции в URL не попало (гео-шаг живёт внутри `enrich`, а
   при выключенном ИИ ветка завершается до сужения). Пользователь ждёт ссылку
   «ближе к МГУ», получает общую.

7. **«кирпичный» без слова «дом» не попал в каталог материалов (запрос 30).**
   «панельный дом» → корректный warning «материал не поддерживается pik.ru», а
   соседнее «кирпичный» (без «дом» рядом) → generic «не удалось распознать».
   Непоследовательно: клиенту два разных объяснения об одном и том же классе.

8. **Маркер «недалеко от» утекает в warning (запрос 19).** POI-парк распознан,
   но грамматический маркер близости «недалеко от» отдельно попадает в
   «нераспознанное» — шум в warnings (сам POI при этом извлечён верно).

### ⚠️ Ограничения окружения/данных (не баги кода)

- **ИИ выключен** (`AI_ENRICHMENT_ENABLED=false`) — запросы с POI (16, 17, 19,
  20) корректно уходят в warning «ИИ-обогащение выключено», ничего не выдумывая.
- **Неполные справочники метро/районов** — «Тульская», «ЦСКА», «Аэропорт»,
  «Хамовники» распознаются как сущности, но без GUID/id/slug не попадают в URL
  (docкуривка вручную, см. CLAUDE.md, раздел «Справочники»). Неоднозначность
  «Аэропорт»/«Аэропорт Внуково» — тоже следствие данных.
- **Нет POI-категории «поликлиника/клиника»** (запрос 20) и распознавания
  «возле метро» — честно уходят в warnings.
- **Толерантность к опечаткам ограничена** (запрос 25): «митро», «падешевле» не
  распознаны; это NLU за пределами regex-парсера — корректно в warnings, не
  додумано.

## Итог

Из 30 запросов: **12 ✅ корректно**, **10 ⚠️ ограничения окружения/данных**,
**5 🟡 недочётов**, **3 🔴 дефекта**. Базовый детерминированный пайплайн
устойчив (комнатность, цена в смешанных единицах, отделка, этажи, отрицания,
каталог неподдерживаемого работают). Приоритет на починку: запрос 06 (ложный
этаж из цены), запрос 28 (ложный `manybathrooms`), запрос 03 (потеря цены).
Правки в `app/` в рамках этой задачи не вносились.
