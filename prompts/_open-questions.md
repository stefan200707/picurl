# Открытые вопросы (решать по ходу milestone, фиксировать в CLAUDE.md)

Эти вопросы не блокируют старт, но должны быть закрыты к соответствующему шагу.
Когда принято решение — **запиши его в `CLAUDE.md`** и, при необходимости, поправь
соответствующий промпт/тесты.

| # | Вопрос | Где решаем | Дефолт (если не решено иначе) |
|---|--------|-----------|-------------------------------|
| 1 | Дефолтная сортировка, если пользователь её не указал (цена ↑? «рекомендуемые»?) | 01 / 07 | не добавлять `sortBy/orderBy` вовсе (= «рекомендуемые» на сайте) |
| 2 | Поведение при нулевой выдаче: автоослабление критерия vs явное «нет вариантов» | 08 / 09 | вернуть `result_count: 0` + флаг/warning; автоослабление — как опциональная стратегия за флагом |
| 3 | Поддержка вторичного рынка | 04 | `pik.ru` — только новостройки; «вторичку» игнорировать с warning |
| 4 | Порог доверия rapidfuzz (score) | 05 | старт `score ≥ 85`, при триггер-предлоге порог можно снизить; подобрать эмпирически |
| 5 | Несколько кандидатов с близким score («Юго-Западная»/«Юго-Восточная») | 05 | вернуть топ-1 + warning с альтернативами; explicit-вопрос — как расширение |
| 6 | Эмбеддинги/векторный поиск | — | решено иначе: см. промпт 15 — эмбеддинги/векторный поиск теперь in-scope (v2), но только как fallback-слой, не замена детерминированного парсинга |

## Открытые вопросы v2 (ИИ-обогащение)

| # | Вопрос | Где решаем |
|---|--------|-----------|
| 7 | Эмбеддинг-модель: конкретный выбор и размерность вектора? | 16 |
| 8 | Источник POI-данных (школы, парки): карточки `pik.ru` vs внешняя БД (OSM/Yandex), лицензии? | 16/17 |
| 9 | Численный радиус или критерий "центра Москвы" для гео-вычислений? | 17 |
| 10| Порог промоушена факта в детерминированный слой: как понимать, что LLM права? | 19/20 |
| 11| Выбор LLM для извлечения параметров (Claude 3 Haiku vs Sonnet) и бюджет/ограничения? | 18 |

## Мелкое, не блокер (собрать заодно со справочниками на шаге 03)

- Полный список слагов `optionGroups` / `options` / `currentBenefit`
  (собрано по одному примеру на категорию — нужно пройтись по всем чекбоксам).
- «Тип паркинга» — поле рядом с «Время до метро», поведение не проверено.
- Поведение при выборе 2+ ЖК одновременно (вероятно `blocks=id,id` по общей
  закономерности single-путь/multi-query, но не подтверждено вживую).
