# Опустошённые (deprecated) файлы

Файлы ниже опустошены (0 байт) в рамках очистки неиспользуемых markdown-файлов
репозитория. Каждый подтверждён `grep` по всему дереву (включая `.md`, код,
`CLAUDE.md`, `README.md`, `prompts/**`, `docs/**`) — ни одной входящей ссылки/
упоминания нигде, кроме самого файла и (для части из них) обсуждения в
`AUDIT_REPORT.md` как кандидата на удаление. Человеку нужно физически убрать их
из репозитория командой `git rm <путь>`.

| Путь | Причина | Дата |
|---|---|---|
| `audit.md` | Более ранний ad-hoc аудит проекта прошлой агентской сессии. Часть находок уже неактуальна (кэш `build_choices()` уже добавлен, инвалидация кэша справочников уже реализована через `/internal/refresh-dicts`). Не упомянут в `CLAUDE.md`/`README.md`/skills, ноль входящих ссылок. | 2026-07-22 |
| `center_report.md` | Разовый ответ на вопрос пользователя «почему ИИ не нашёл квартиру в центре» из прошлой сессии. Не документация проекта, ноль входящих ссылок откуда-либо. | 2026-07-22 |
| `explanation.md` | Разовое объяснение точечного фикса (смена `AI_MODEL_NAME`) из прошлой сессии. Ноль входящих ссылок, не часть карты документации проекта. | 2026-07-22 |
| `report.md` | Разовый отчёт об исправлении конкретного бага с распознаванием «центра» — содержание уже отражено в `CLAUDE.md` (разделы «Аудит и починка слоя данных ИИ» и «Стресс-тестирование парсера»). Ноль входящих ссылок. | 2026-07-22 |
| `recent_changes_last_2_hours.md` | Снэпшот статуса «за последние 2 часа» конкретной прошлой сессии — по определению быстро устаревающий контент, не входит в документацию проекта. Ноль входящих ссылок. | 2026-07-22 |
| `check.py` | Одноразовый скрипт сверки GUID метро с `metro.json` (сам помечен «Не часть рантайма/прод-образа»). Не импортируется. `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `refactor_entity_match.py` | Незавершённый черновик рефакторинга (4 строки, ничего не делает). `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `run_stress_test.py` | Ad-hoc раннер ручного стресс-теста (результаты уже в `CLAUDE.md`). Не в pytest-сьюте. `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `scratch.py` | Scratch-файл одной отладочной сессии (сценарий «центр» уже покрыт тестами). `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `test_overpass.py` | Ручной ad-hoc тест доступности Overpass API (не pytest, вне `tests/`). `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `test_regex.py` | Ручная проверка одного regex-паттерна (не pytest, вне `tests/`). `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `update_cache.py` | Разовый скрипт дописывания slug'ов в `poi_cache.json` (результат уже закоммичен). `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `update_json.py` | Разовый скрипт первичного наполнения `option_groups.json`/`options.json` (данные уже в справочниках). `AUDIT_REPORT.md` 5.1. | 2026-07-23 |
| `stress_test_output.txt` | Застывший stdout-лог одного прогона стресс-теста, устаревает при любой правке парсера. `AUDIT_REPORT.md` 5.2. | 2026-07-23 |
