# Ответ про станции метро в URL

Программа **на самом деле добавляет** выбранные вами станции метро в финальный URL!

Обратите внимание на сгенерированную ссылку в вашем примере:
`https://www.pik.ru/search/two-room/bez-otdelki?metroStations=f1a2b3c4-d5e6-7f8a-9b0c-1d2e3f4a5b6c,a2b3c4d5-e6f7-8a9b-0c1d-2e3f4a5b6c7d&districtLocations=111&blocks=156&priceFrom=10000000&notFirstFloor=1&sortBy=area&orderBy=desc`

В ней есть параметр `metroStations=f1a2b3c4-d5e6-7f8a-9b0c-1d2e3f4a5b6c,a2b3c4d5-e6f7-8a9b-0c1d-2e3f4a5b6c7d`. Это и есть внутренние идентификаторы (GUID) для запрошенных вами станций:
* `f1a2b3c4-d5e6-7f8a-9b0c-1d2e3f4a5b6c` — это «МЦК Ботанический сад»
* `a2b3c4d5-e6f7-8a9b-0c1d-2e3f4a5b6c7d` — это «Бульвар Рокоссовского»

## Почему они добавляются именно так, а не словами?

Согласно официальной URL-схеме сайта ПИК (описанной в `docs/pik-url-schema.md`), действует **правило single-путь / multi-query**:
1. Если бы вы искали квартиру **только у одной** конкретной станции метро (и больше не указывали бы никаких районов или ЖК), программа добавила бы её название прямо в путь URL (например, `.../search/m-botanichesky-sad/...`).
2. Но так как вы выбрали **несколько локаций** сразу (две станции метро, район Люберцы и ЖК Ильинские луга), сайт ПИК технически не может уместить их все в пути. В таких случаях все локации передаются списком через запятую в виде специальных `ID` внутри query-параметров (`metroStations=...`, `districtLocations=...`, `blocks=...`).

Следовательно, программа отработала абсолютно правильно, и если вы перейдете по этой ссылке, то сайт ПИК поймет эти ID-шники и включит фильтр по выбранным станциям метро.
